# Go Worker Dashboard UI

This document was consolidated to avoid drift.

Use:
- `docs/GO_WORKER_CONTROL_PLANE_SPEC.md` for current runtime + UI behavior.
- `docs/archive/GO_WORKER_DASHBOARD_UI.md` for the historical standalone UI snapshot.
